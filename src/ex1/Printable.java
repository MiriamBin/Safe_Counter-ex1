package ex1;

/**
 * The Printable interface is an interface that provides a `print()` method that can be implemented by classes
 * that need to print information to the console or another output stream.
 */
public interface Printable {
    /**
     * Prints information about the object to the console.
     */
    void print();
}
